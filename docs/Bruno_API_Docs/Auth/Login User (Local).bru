meta {
  name: Login User (Local)
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}
