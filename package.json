{
  "name": "appministrador_server",
  "version": "1.0.0",
  "description": "Server for AppMinistrador App",
  "main": "index.js",
  "scripts": {
    "prisma-introspection": "npx prisma db pull",
    "prisma-baseline-1": "rimraf prisma/migrations && mkdir -p prisma/migrations/0_init",
    "prisma-baseline-2": "npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/0_init/migration.sql",
    "prisma-baseline-3": "npx prisma migrate resolve --applied 0_init",
    "prisma-generate": "npx prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && npx tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "predev": "npm run build",
    "dev": "concurrently \"npx tsc -w\" \"nodemon dist/index.js\""
  },
  "author": "Renzo Belon",
  "license": "ISC",
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "@typescript-eslint/eslint-plugin": "^7.3.1",
    "@typescript-eslint/parser": "^7.3.1",
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0",
    "prisma": "^5.11.0",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "concurrently": "^8.2.2",
    "date-fns": "^3.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "rimraf": "^5.0.5"
  }
}
